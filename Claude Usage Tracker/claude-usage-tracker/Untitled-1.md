IDEAS

- make sure extra usage is correctly tracked (this is alot easier)

&#x09;- track if it is on or off too

\- make sure al errors are logged to trouble shoot (with full error messages orm both running node process + api calls we ake)

\- creae dcos on how ot search the sql database

\- for useage graph add views for dif days, weeks, months, or just current window

\- host dashboard through a tunnel so i can see the stats on laptop

\- move database to supabase and improve analsictics to gt exact data form caude code usage on both laptop and pc

\- The Claude usage monitor needs to keep pinging at the interval until the delta is lower than 1%. So if the Delta is equal to 1%, it should keep pinging it at that rate. (I think there's already a caveat, though. If it's moving at all, it should go down to 30%. If it's moving more than 1%, it drops down immediately to every 30 seconds. Which is it?)

