---
phase: 03-sender-module
plan: 03
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified: [src/app/api/send-now/route.ts]
autonomous: true
requirements: [SEND-01, SEND-05]
user_setup: []
must_haves:
  truths:
    - "POST /api/send-now route exports dynamic = 'force-dynamic' for no caching"
    - "POST /api/send-now calls send(config) with default options, triggering a manual send"
    - "Route returns the send_log row as JSON on success"
    - "Route returns 500 error with error message on failure"
    - "Manual sends write scheduled_for=NULL and is_anchor=0 (per D-05)"
  artifacts:
    - path: "src/app/api/send-now/route.ts"
      provides: "POST handler calling send(config) and returning SendLogRow as JSON"
      min_lines: 20
  key_links:
    - from: "src/app/api/send-now/route.ts POST handler"
      to: "src/lib/sender.ts send()"
      via: "function call with config"
      pattern: "import.*send.*from.*sender"
    - from: "POST /api/send-now"
      to: "send_log table"
      via: "send() calls insertSendLog()"
      pattern: "send\\(config\\)"
---

<objective>
Create POST /api/send-now route that triggers a manual send via the sender module and returns the send_log result. This enables dashboard testing and manual send invocation (Phase 5).

Purpose: First invocation point for the sender; allows testing send() function end-to-end via HTTP before Phase 4 scheduler integrates it.

Output: New src/app/api/send-now/route.ts with POST handler following Next.js conventions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md — Phase 3 goal § Success Criteria
@.planning/REQUIREMENTS.md — SEND-01, SEND-05 requirement definitions
@.planning/phases/03-sender-module/03-CONTEXT.md — D-05 locked decision (manual fires write scheduled_for=NULL, is_anchor=0)
@.planning/phases/03-sender-module/03-RESEARCH.md — Pattern 3: API Route Handler code example
@.planning/phases/03-sender-module/03-PATTERNS.md — API route handler pattern from src/app/api/dashboard/route.ts (lines 195–240)

Dependencies:
- src/lib/sender.ts (Plan 02, completed) — provides send() function
- src/lib/config.ts (Phase 1, completed) — provides getConfig(), Config type
- src/lib/db.ts (Phase 1, completed) — provides SendLogRow type
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create POST /api/send-now route handler</name>
  <files>src/app/api/send-now/route.ts</files>
  <read_first>
    1. src/app/api/dashboard/route.ts — Read entire file to understand route handler pattern: dynamic export, function structure, error handling, NextResponse.json(), bracketed logging.
    2. src/lib/sender.ts — Read send() function signature to understand options and return type.
    3. src/lib/config.ts — Read to understand getConfig() usage.
  </read_first>
  <action>
    Create src/app/api/send-now/route.ts with the following exact structure:

    ```typescript
    import { NextResponse } from "next/server";
    import { getConfig } from "@/lib/config";
    import { send } from "@/lib/sender";

    export const dynamic = "force-dynamic";

    export async function POST() {
      const config = getConfig();
      try {
        const result = await send(config);
        return NextResponse.json(result);
      } catch (err) {
        console.error("[send-now]", err instanceof Error ? err.message : String(err));
        return NextResponse.json(
          { error: "Failed to send message" },
          { status: 500 }
        );
      }
    }
    ```

    **Key details:**
    - Export `const dynamic = "force-dynamic"` to disable Next.js caching (every request triggers a fresh send)
    - Named export `async function POST()` — Next.js convention for HTTP method handlers
    - Call `send(config)` with no additional options — defaults to 60s timeout, manual-fire (scheduled_for=null, is_anchor=0)
    - Return `NextResponse.json(result)` on success (200 by default)
    - Catch errors and return `NextResponse.json({ error: ... }, { status: 500 })`
    - Log errors with `[send-now]` prefix for easy filtering
    - No request body parsing needed (D-05: manual sends use defaults)

    **Why this pattern:**
    - dynamic=force-dynamic: ensures every POST triggers a new send (no static caching)
    - No options passed to send(): uses all defaults (timeoutMs=60_000, scheduledFor=null, isAnchor=0)
    - Simple error handling: catch Error and return 500
  </action>
  <verify>
    <automated>grep -n "export const dynamic" "src/app/api/send-now/route.ts" && grep -n "export async function POST" "src/app/api/send-now/route.ts" && grep -n "send(config)" "src/app/api/send-now/route.ts"</automated>
  </verify>
  <done>
    src/app/api/send-now/route.ts created with:
    - dynamic = "force-dynamic" exported
    - POST() handler defined
    - Calls send(config) with default options (manual fire: scheduled_for=null, is_anchor=0)
    - Returns SendLogRow as JSON on success
    - Returns 500 + error message on catch
    - [send-now] prefix on error logs
    - No lint errors: `npm run lint` passes
    - No build errors: `npm run build` succeeds
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    POST /api/send-now route that manually triggers a send and returns the send_log row as JSON.
  </what-built>
  <how-to-verify>
    1. Run the dev server: `npm run dev` (or `npm run start:prod`)
    2. Open terminal/PowerShell and test the endpoint:
       ```bash
       curl -X POST http://localhost:3017/api/send-now
       ```
       (or `curl -X POST http://127.0.0.1:3018/api/send-now` for prod)
    3. Expected response (JSON): A send_log row with fields:
       - id: number > 0
       - fired_at: ISO 8601 timestamp
       - scheduled_for: null (manual fire)
       - is_anchor: 0
       - status: "ok", "error", or "timeout" (depending on claude CLI availability)
       - duration_ms: number >= 0
       - question: non-empty string from QUESTIONS constant
       - response_excerpt: string (first 500 chars) or null
       - error_message: string or null

    4. Verify in database (optional): Query SQLite:
       ```bash
       sqlite3 data/usage.db "SELECT id, fired_at, status, question FROM send_log ORDER BY id DESC LIMIT 1;"
       ```
       Should show one row matching the JSON response.

    **Success criteria:**
    - curl returns 200 + JSON with SendLogRow fields
    - scheduled_for is null (manual fire)
    - is_anchor is 0
    - Row exists in send_log table
    - status is one of: ok, error, timeout (depending on environment)
  </how-to-verify>
  <resume-signal>
    Confirmed working: POST /api/send-now returns 200 + SendLogRow JSON
    OR describe any issues encountered
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP endpoint on 127.0.0.1 | send-now route binds to 127.0.0.1:3018 in prod (Phase 6), localhost:3017 in dev; not public |
| No user input | POST body not parsed; send() uses hardcoded QUESTIONS constant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | D (Denial of Service) | Repeated POST calls exhaust sending quota | accept | Rate limiting deferred to Phase 4 scheduler (only fires at scheduled times). Phase 5 dashboard can add UI rate-limit on button clicks. |
| T-03-09 | I (Information Disclosure) | SendLogRow returned as JSON includes response_excerpt | accept | Local-only endpoint (127.0.0.1); no network exposure. Response is cached in send_log table (audit trail). |

</threat_model>

<verification>
Phase 3 goal: Manual sends are invokable via HTTP POST, return send_log row, and write database records correctly.

1. Route exists: src/app/api/send-now/route.ts with POST export.
2. dynamic=force-dynamic: Ensures fresh send on every POST (no caching).
3. send(config) called: With default options (manual fire defaults).
4. SendLogRow returned: JSON response includes all 9 fields.
5. Manual fire distinction: scheduled_for=null, is_anchor=0.
6. Database written: send_log row persisted by insertSendLog().
7. Error handling: 500 returned on exception.
</verification>

<success_criteria>
- src/app/api/send-now/route.ts created: POST handler, 20+ lines.
- Route follows Next.js conventions: dynamic export, named POST function, NextResponse.json().
- Verified via manual curl test: POST returns 200 + SendLogRow JSON.
- send_log row persisted: Database query confirms row inserted.
- scheduled_for=null and is_anchor=0: Manual fire distinction verified.
- Error handling works: 500 returned if send() throws.
- Lint passes: `npm run lint` succeeds.
- Build passes: `npm run build` succeeds.
</success_criteria>

<output>
After completion, create `.planning/phases/03-sender-module/03-03-SUMMARY.md` documenting:
- File created: src/app/api/send-now/route.ts
- Lines: ~20 (route handler)
- POST handler: Calls send(config), returns SendLogRow, handles errors
- Verification: Manual curl test successful
- Decision honored: D-05 (manual fires with scheduled_for=null, is_anchor=0)
</output>
