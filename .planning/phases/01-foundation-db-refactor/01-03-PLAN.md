---
phase: 01-foundation-db-refactor
plan: 03
type: execute
wave: 2
depends_on:
  - "01-02"
files_modified:
  - "claude-usage-tracker/src/lib/queries.ts"
  - "claude-usage-tracker/test/queries.test.ts"
autonomous: true
requirements:
  - DATA-06

must_haves:
  truths:
    - "queries.ts exists with exported ParsedSnapshot interface, parseSnapshot function, and parseSnapshots function"
    - "parseSnapshot correctly handles cookie-auth raw_json (usage key wraps inner payload)"
    - "parseSnapshot correctly handles bearer-auth raw_json (bare usage object at root)"
    - "parseSnapshot converts extra_usage monetary fields from cents to dollars"
    - "parseSnapshot returns null for all derived fields when raw_json is null or malformed"
    - "queries.test.ts covers all 8 test cases from the validation strategy"
  artifacts:
    - path: "claude-usage-tracker/src/lib/queries.ts"
      provides: "ParsedSnapshot interface, parseSnapshot, parseSnapshots functions"
      min_lines: 60
      exports:
        - "ParsedSnapshot"
        - "parseSnapshot"
        - "parseSnapshots"
    - path: "claude-usage-tracker/test/queries.test.ts"
      provides: "unit test coverage for DATA-06"
      min_lines: 100
  key_links:
    - from: "claude-usage-tracker/src/lib/queries.ts"
      to: "claude-usage-tracker/src/lib/normalize.ts"
      via: "import { normalizeUsagePayload } from ./normalize"
      pattern: "normalizeUsagePayload"
    - from: "claude-usage-tracker/src/lib/queries.ts"
      to: "claude-usage-tracker/src/lib/db.ts"
      via: "import type { SnapshotRow } from ./db"
      pattern: "SnapshotRow"
---

<objective>
Create `claude-usage-tracker/src/lib/queries.ts` as the new read-side module (per D-06) and create `claude-usage-tracker/test/queries.test.ts` covering all DATA-06 test cases. The module parses `raw_json` from a `SnapshotRow` and returns a `ParsedSnapshot` with the same field names as the old typed columns — so that `analysis.ts` can switch to it with minimal changes in Plan 05.

Purpose: This is the Wave 0 gap for DATA-06. Without `queries.ts`, the analysis layer has no way to derive typed values from `raw_json`.
Output: `queries.ts` (new file) and `queries.test.ts` (new file, full coverage).
</objective>

<execution_context>
@/c/Users/2supe/.claude/get-shit-done/workflows/execute-plan.md
@/c/Users/2supe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation-db-refactor/01-CONTEXT.md
@.planning/phases/01-foundation-db-refactor/01-RESEARCH.md
@.planning/phases/01-foundation-db-refactor/01-PATTERNS.md

<interfaces>
<!-- Contracts queries.ts must implement -->

Import dependencies:
  import { normalizeUsagePayload } from "./normalize";
  import type { SnapshotRow } from "./db";

ParsedSnapshot interface (export):
  id: number
  timestamp: string
  status: string
  endpoint: string | null
  response_status: number | null
  error_message: string | null
  raw_json: string | null
  five_hour_utilization: number | null
  five_hour_resets_at: string | null
  seven_day_utilization: number | null
  seven_day_resets_at: string | null
  extra_usage_enabled: boolean | null
  extra_usage_monthly_limit: number | null   (dollars, not cents)
  extra_usage_used_credits: number | null    (dollars, not cents)
  extra_usage_utilization: number | null

normalizeUsagePayload signature (from normalize.ts):
  normalizeUsagePayload(payload: Record<string, unknown>): NormalizedPayload
  NormalizedPayload.windows: Array<{ key: string; utilization: number; resetsAt: string; ... }>
  NormalizedPayload.extraUsage: { isEnabled: boolean; monthlyLimit: number; usedCredits: number; utilization: number } | null

Cookie-auth raw_json structure: { usage: { five_hour: {...}, seven_day: {...} }, overage_spend_limit: ..., ... }
Bearer-auth raw_json structure: { five_hour: { utilization: number, resets_at: string }, seven_day: {...} }
Demo raw_json structure: { five_hour: { utilization: number, resets_at: string }, seven_day: {...} }
Null/error raw_json: null -> all derived fields are null

Cents-to-dollars rule: extra_usage.monthly_limit and extra_usage.used_credits come from the API in cents.
  Conversion: Math.round(cents) / 100

safeParseRaw private helper:
  if raw is null or empty string -> return null
  try JSON.parse(raw) -> return parsed object
  catch -> return null
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create queries.test.ts with all DATA-06 test cases</name>
  <read_first>
    - claude-usage-tracker/src/lib/normalize.ts (full file — understand normalizeUsagePayload input/output and isUsageBucket duck-type check so test payloads are shaped correctly)
    - claude-usage-tracker/test/analysis.test.ts (lines 1-10 — copy the node:test import pattern exactly)
  </read_first>
  <files>
    claude-usage-tracker/test/queries.test.ts
  </files>
  <behavior>
    All tests run against parseSnapshot imported from "../src/lib/queries". The file does not need a real database.
    Test cases (8 for parseSnapshot, 2 for parseSnapshots):

    parseSnapshot tests:
    1. null raw_json -> all derived fields (five_hour_utilization, five_hour_resets_at, seven_day_utilization, seven_day_resets_at, extra_usage_enabled, extra_usage_monthly_limit, extra_usage_used_credits, extra_usage_utilization) are null; identity fields (id, timestamp, status) passthrough correctly
    2. bearer-auth payload (bare five_hour/seven_day at root) -> five_hour_utilization = 55.0, five_hour_resets_at = "2026-04-19T15:00:00.000Z", seven_day_utilization = 20.0
    3. cookie-auth payload (usage key wraps inner object) -> five_hour_utilization = 42.5, five_hour_resets_at = "2026-04-19T15:00:00.000Z", seven_day_utilization = 30.0
    4. demo payload (same shape as bearer) -> five_hour_utilization = 30.0, seven_day_utilization = 25.0
    5. extra_usage cents-to-dollars: input monthly_limit=1000, used_credits=283 in cents -> output extra_usage_monthly_limit = 10.00, extra_usage_used_credits = 2.83
    6. no extra_usage key in payload -> extra_usage_enabled, extra_usage_monthly_limit, extra_usage_used_credits, extra_usage_utilization all null
    7. malformed JSON string (e.g., "not-json") -> all derived fields null, no throw
    8. extra_usage with is_enabled=true -> extra_usage_enabled = true (boolean, not 1)

    parseSnapshots tests:
    9. empty array -> returns []
    10. array of 2 rows -> returns array of 2 ParsedSnapshot objects
  </behavior>
  <action>
    Create claude-usage-tracker/test/queries.test.ts. This test file tests a pure function — no DB, no temp file, no teardown needed.

    File structure:
    - Import: import { describe, it } from "node:test";
    - Import: import assert from "node:assert/strict";
    - Import: import { parseSnapshot, parseSnapshots } from "../src/lib/queries";
    - Import: import type { SnapshotRow } from "../src/lib/db";

    Define a makeRawRow helper function:
      function makeRawRow(overrides: Partial<SnapshotRow> & { timestamp: string }): SnapshotRow {
        return {
          id: 1,
          timestamp: overrides.timestamp,
          status: "ok",
          endpoint: "test",
          response_status: 200,
          raw_json: null,
          error_message: null,
          ...overrides,
        };
      }

    Define payload constants for use in tests:
      bearerRaw: JSON.stringify({ five_hour: { utilization: 55.0, resets_at: "2026-04-19T15:00:00.000Z" }, seven_day: { utilization: 20.0, resets_at: "2026-04-26T10:00:00.000Z" } })
      cookieRaw: JSON.stringify({ usage: { five_hour: { utilization: 42.5, resets_at: "2026-04-19T15:00:00.000Z" }, seven_day: { utilization: 30.0, resets_at: "2026-04-26T10:00:00.000Z" } }, overage_spend_limit: null })
      demoRaw: JSON.stringify({ five_hour: { utilization: 30.0, resets_at: "2026-04-19T15:00:00.000Z" }, seven_day: { utilization: 25.0, resets_at: "2026-04-26T10:00:00.000Z" } })
      extraUsageRaw: JSON.stringify({ five_hour: { utilization: 80.0, resets_at: "2026-04-19T15:00:00.000Z" }, extra_usage: { is_enabled: true, monthly_limit: 1000, used_credits: 283, utilization: 28.3 } })
      noExtraUsageRaw: JSON.stringify({ five_hour: { utilization: 50.0, resets_at: "2026-04-19T15:00:00.000Z" } })

    Write describe("parseSnapshot") with 8 it() blocks covering all behaviors listed above.
    Write describe("parseSnapshots") with 2 it() blocks.

    Note: the queries.ts module does NOT exist yet when this test file is created. The test file will fail to import until Task 2 creates queries.ts. That is expected — the tests are written first (TDD red phase).

    Code style: 2-space indent, double quotes, semicolons.
  </action>
  <verify>
    <automated>
      cd "claude-usage-tracker" && npx tsx --test test/queries.test.ts 2>&1 | head -5
      # Expected: import error (queries.ts does not exist yet) — this is the TDD RED state, which is correct
    </automated>
  </verify>
  <acceptance_criteria>
    - File claude-usage-tracker/test/queries.test.ts exists.
    - grep -c "it(" claude-usage-tracker/test/queries.test.ts returns 10 or more (all test cases present).
    - grep -n "parseSnapshot\|parseSnapshots" claude-usage-tracker/test/queries.test.ts shows imports and test calls.
    - grep -n "cookieRaw\|bearerRaw\|demoRaw\|extraUsageRaw" claude-usage-tracker/test/queries.test.ts shows all payload fixtures present.
    - grep -n "cents.*dollars\|monthly_limit.*10\|used_credits.*2.83" claude-usage-tracker/test/queries.test.ts or assertions verifying extra_usage_monthly_limit = 10 and extra_usage_used_credits = 2.83 are present.
    - grep -n "null.*raw_json\|raw_json.*null" claude-usage-tracker/test/queries.test.ts or a test for null raw_json producing null derived fields is present.
    - grep -n "malformed\|not-json" claude-usage-tracker/test/queries.test.ts shows malformed JSON test case is present.
  </acceptance_criteria>
  <done>queries.test.ts exists with 10 test cases covering all DATA-06 behaviors. File fails import (queries.ts does not exist yet) — RED state is correct.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create queries.ts — ParsedSnapshot, parseSnapshot, parseSnapshots</name>
  <read_first>
    - claude-usage-tracker/src/lib/normalize.ts (full file — understand normalizeUsagePayload return shape, NormalizedPayload type, how windows array is keyed, and extraUsage shape)
    - claude-usage-tracker/src/lib/db.ts (updated — confirm the new SnapshotRow 7-field interface this module imports)
    - claude-usage-tracker/test/queries.test.ts (created in Task 1 — implement until all tests pass)
  </read_first>
  <files>
    claude-usage-tracker/src/lib/queries.ts
  </files>
  <behavior>
    Implementation passes all 10 tests in queries.test.ts:
    - null raw_json -> all 8 derived fields are null
    - bearer payload -> five_hour_utilization and seven_day_utilization extracted from root-level keys
    - cookie payload -> usage key unwrapped before calling normalizeUsagePayload
    - demo payload -> same as bearer (five_hour/seven_day at root)
    - extra_usage cents-to-dollars: Math.round(cents) / 100 applied to monthly_limit and used_credits
    - absent extra_usage key -> all extra_usage derived fields null
    - malformed JSON -> no throw, all derived fields null
    - is_enabled: true -> extra_usage_enabled: true (boolean)
    - parseSnapshots([]) -> []
    - parseSnapshots([row1, row2]) -> [ParsedSnapshot, ParsedSnapshot]
  </behavior>
  <action>
    Create claude-usage-tracker/src/lib/queries.ts. This is a pure-function module — no side effects, no DB access.

    Imports (relative to sibling modules, per CLAUDE.md import convention):
      import { normalizeUsagePayload } from "./normalize";
      import type { SnapshotRow } from "./db";

    Export the ParsedSnapshot interface with exactly these fields:
      id: number
      timestamp: string
      status: string
      endpoint: string | null
      response_status: number | null
      error_message: string | null
      raw_json: string | null
      five_hour_utilization: number | null
      five_hour_resets_at: string | null
      seven_day_utilization: number | null
      seven_day_resets_at: string | null
      extra_usage_enabled: boolean | null
      extra_usage_monthly_limit: number | null
      extra_usage_used_credits: number | null
      extra_usage_utilization: number | null

    Private module-local helper:
      function safeParseRaw(raw: string | null): Record<string, unknown> | null
        - if !raw return null
        - try { return JSON.parse(raw); } catch { return null; }

    Export parseSnapshot(row: SnapshotRow): ParsedSnapshot
      Implementation steps:
      1. Call safeParseRaw(row.raw_json) -> payload
      2. Detect cookie vs bearer: if payload has "usage" key, unwrap -> usagePayload = payload.usage as Record<string, unknown>. Otherwise usagePayload = payload ?? {}
      3. Call normalizeUsagePayload(usagePayload) -> normalized
      4. Find five_hour window: normalized.windows.find((w) => w.key === "five_hour")
      5. Find seven_day window: normalized.windows.find((w) => w.key === "seven_day")
      6. Get eu = normalized.extraUsage
      7. Define centsToDollars as arrow function: (v: number | null) => v == null ? null : Math.round(v) / 100
      8. Return ParsedSnapshot object with:
         - Identity fields (passthrough): id, timestamp, status, endpoint, response_status, error_message, raw_json
         - five_hour_utilization: fiveHour?.utilization ?? null
         - five_hour_resets_at: fiveHour?.resetsAt ?? null
         - seven_day_utilization: sevenDay?.utilization ?? null
         - seven_day_resets_at: sevenDay?.resetsAt ?? null
         - extra_usage_enabled: eu ? eu.isEnabled : null
         - extra_usage_monthly_limit: eu ? centsToDollars(eu.monthlyLimit) : null
         - extra_usage_used_credits: eu ? centsToDollars(eu.usedCredits) : null
         - extra_usage_utilization: eu?.utilization ?? null

    Export parseSnapshots(rows: SnapshotRow[]): ParsedSnapshot[]
      return rows.map(parseSnapshot);

    Code style: named exports only, no default export, function keyword for top-level named functions, arrow functions for centsToDollars and safeParseRaw callbacks, 2-space indent, double quotes, semicolons.
  </action>
  <verify>
    <automated>cd "claude-usage-tracker" && npx tsx --test test/queries.test.ts 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - cd claude-usage-tracker && npx tsx --test test/queries.test.ts exits with code 0 (all 10 tests pass).
    - grep -n "export interface ParsedSnapshot" claude-usage-tracker/src/lib/queries.ts returns 1 match.
    - grep -n "export function parseSnapshot" claude-usage-tracker/src/lib/queries.ts returns 1 match.
    - grep -n "export function parseSnapshots" claude-usage-tracker/src/lib/queries.ts returns 1 match.
    - grep -n "normalizeUsagePayload" claude-usage-tracker/src/lib/queries.ts returns at least 2 matches (import + call).
    - grep -n "centsToDollars" claude-usage-tracker/src/lib/queries.ts returns at least 2 matches (definition + usage).
    - grep -n "usage.*in.*payload\|payload.*usage" claude-usage-tracker/src/lib/queries.ts or the cookie-auth unwrap logic for the "usage" key is present.
    - grep -n "default export" claude-usage-tracker/src/lib/queries.ts returns nothing (named exports only).
    - cd claude-usage-tracker && npx tsc --noEmit 2>&1 | grep "src/lib/queries.ts:" returns nothing (no TypeScript errors in queries.ts itself).
  </acceptance_criteria>
  <done>queries.ts exists with ParsedSnapshot, parseSnapshot, parseSnapshots. All 10 tests in queries.test.ts pass (exit 0). TypeScript compiles queries.ts without errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw_json -> parsed object | JSON.parse is wrapped in try/catch; malformed input returns null, never throws |
| API payload structure | parseSnapshot handles both cookie-auth (usage key) and bearer-auth (bare payload) — unknown top-level structure falls through to normalizeUsagePayload which is tolerant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | safeParseRaw JSON.parse | accept | Input is read from SQLite (local disk, user-controlled), not from network directly; JSON.parse in try/catch prevents prototype pollution via throw suppression |
| T-03-02 | Information Disclosure | ParsedSnapshot field passthrough | accept | ParsedSnapshot fields are derived from locally-stored data; no new disclosure surface |
| T-03-03 | Denial of Service | large raw_json strings | accept | Local desktop app; JSON.parse on a large string is bounded by available memory; no mitigation needed |
</threat_model>

<verification>
After both tasks complete:
1. cd claude-usage-tracker && npx tsx --test test/queries.test.ts → exit 0
2. grep -c "export" claude-usage-tracker/src/lib/queries.ts → 3 (ParsedSnapshot, parseSnapshot, parseSnapshots)
3. cd claude-usage-tracker && npx tsx --test test/db.test.ts test/queries.test.ts → both pass
</verification>

<success_criteria>
- queries.ts exports ParsedSnapshot interface, parseSnapshot function, parseSnapshots function.
- All 10 test cases in queries.test.ts pass (exit 0).
- TypeScript compiles queries.ts without errors.
- No default export in queries.ts.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-db-refactor/01-03-SUMMARY.md` following the summary template.
</output>
