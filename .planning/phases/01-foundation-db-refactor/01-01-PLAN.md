---
phase: 01-foundation-db-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "Claude Message Sender/claude_message_send_with_browser.py"
  - "Claude Message Sender/claude_message_send_with_CC_CLI.py"
  - "Claude Message Sender/requirements.txt"
  - "Claude Message Sender/test_send_now.py"
  - "claude-usage-tracker/.env.local"
autonomous: true
requirements:
  - DEPLOY-06

must_haves:
  truths:
    - "The Claude Message Sender/ directory no longer exists anywhere in the repo"
    - "claude-usage-tracker/.env.local is gone from the working tree"
    - "claude-usage-tracker/ canonical tree is untouched — all 43 tracked files remain"
  artifacts:
    - path: "Claude Message Sender/"
      provides: "directory must NOT exist after deletion"
      contains: "ABSENT"
  key_links:
    - from: "git ls-files"
      to: "Claude Message Sender/"
      via: "git rm -r"
      pattern: "git ls-files.*Claude Message Sender"
---

<objective>
Delete the `Claude Message Sender/` git-tracked directory and the untracked `claude-usage-tracker/.env.local` file from the repository. No code changes, no data preservation — pure filesystem cleanup per D-01.

Purpose: DEPLOY-06 requires the Python sender and the stale untracked env file to be removed before Phase 1 code work begins, so later plans have a clean working tree.
Output: Repository with only the canonical `claude-usage-tracker/` tracker tree and no Python sender artifacts.
</objective>

<execution_context>
@/c/Users/2supe/.claude/get-shit-done/workflows/execute-plan.md
@/c/Users/2supe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation-db-refactor/01-CONTEXT.md
@.planning/phases/01-foundation-db-refactor/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Claude Message Sender directory and stale .env.local</name>
  <read_first>
    - Run `git ls-files "Claude Message Sender/"` to confirm all 4 tracked files before deleting.
    - Run `ls "claude-usage-tracker/.env.local" 2>/dev/null && echo "exists" || echo "absent"` to confirm whether the untracked file is present.
    - Do NOT read the canonical claude-usage-tracker/src/ files — this task does not touch them.
  </read_first>
  <files>
    Claude Message Sender/claude_message_send_with_browser.py
    Claude Message Sender/claude_message_send_with_CC_CLI.py
    Claude Message Sender/requirements.txt
    Claude Message Sender/test_send_now.py
    claude-usage-tracker/.env.local
  </files>
  <action>
    Step 1 — Delete the git-tracked Python sender directory:
      git rm -r "Claude Message Sender/"

    This stages the deletion of all 4 files:
      - Claude Message Sender/claude_message_send_with_browser.py
      - Claude Message Sender/claude_message_send_with_CC_CLI.py
      - Claude Message Sender/requirements.txt
      - Claude Message Sender/test_send_now.py

    Step 2 — Delete the untracked env file (per D-01, no preservation needed):
      rm -f "claude-usage-tracker/.env.local"

    Step 3 — Commit:
      git add -A
      git commit -m "chore(01): delete Claude Message Sender and stale .env.local (DEPLOY-06)"

    Do NOT delete anything inside claude-usage-tracker/src/, claude-usage-tracker/test/, or any git-tracked file under claude-usage-tracker/.
  </action>
  <verify>
    <automated>
      # Verify Claude Message Sender is gone from tracked files
      git ls-files "Claude Message Sender/" | wc -l
      # Expected output: 0

      # Verify .env.local is gone
      ls "claude-usage-tracker/.env.local" 2>/dev/null || echo "DELETED"
      # Expected: DELETED

      # Verify canonical tree still has its tracked files
      git ls-files "claude-usage-tracker/src/" | wc -l
      # Expected: > 0 (must be non-zero, canonical tree intact)
    </automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files "Claude Message Sender/"` outputs nothing (zero lines).
    - `ls "Claude Message Sender/" 2>&1` outputs "No such file or directory" or equivalent.
    - `ls "claude-usage-tracker/.env.local" 2>&1` outputs "No such file or directory".
    - `git ls-files "claude-usage-tracker/src/"` outputs at least 10 file paths.
    - `git status` shows only the deletion of the 4 Claude Message Sender files (all staged and committed).
  </acceptance_criteria>
  <done>The Claude Message Sender/ directory and claude-usage-tracker/.env.local are absent from the repository; claude-usage-tracker/ canonical tree is completely intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git working tree | Only the 4 tracked Python files and 1 untracked file are deleted; no other files are touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | git rm command | accept | Deleting only `Claude Message Sender/` — canonical tree is never addressed by the command; risk is near-zero |
| T-01-02 | Denial of Service | rm -f .env.local | accept | File is untracked and explicitly scoped to `claude-usage-tracker/.env.local`; canonical source files are in `claude-usage-tracker/src/` and never referenced |
</threat_model>

<verification>
After the commit:
1. `git ls-files "Claude Message Sender/"` → empty
2. `ls "claude-usage-tracker/.env.local"` → No such file
3. `git ls-files "claude-usage-tracker/src/" | wc -l` → non-zero
4. `git log --oneline -1` → shows the DEPLOY-06 commit message
</verification>

<success_criteria>
- `Claude Message Sender/` is fully removed from the git index and filesystem.
- `claude-usage-tracker/.env.local` is absent from the filesystem.
- The canonical `claude-usage-tracker/` tracker tree has all its existing tracked files intact.
- One commit on the main branch records the deletion.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-db-refactor/01-01-SUMMARY.md` following the summary template.
</output>
