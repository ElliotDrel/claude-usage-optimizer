---
phase: 01-foundation-db-refactor
plan: 05
type: execute
wave: 3
depends_on:
  - "01-02"
  - "01-03"
files_modified:
  - "claude-usage-tracker/src/lib/analysis.ts"
  - "claude-usage-tracker/src/app/api/dashboard/route.ts"
  - "claude-usage-tracker/test/analysis.test.ts"
autonomous: true
requirements:
  - DATA-06
  - UI-08

must_haves:
  truths:
    - "analysis.ts imports ParsedSnapshot from queries.ts and routes all computation through it"
    - "buildDashboardData accepts ParsedSnapshot[] (not SnapshotRow[]) for all computation"
    - "All 4 dashboard panels (heatmap, hourly bars, usage timeline, extra-usage card) compute from ParsedSnapshot fields"
    - "dashboard/route.ts calls parseSnapshots(querySnapshots(config)) before passing to buildDashboardData"
    - "analysis.test.ts makeSnapshot factory returns ParsedSnapshot and all existing tests pass"
    - "npm test exits 0 — full suite green"
  artifacts:
    - path: "claude-usage-tracker/src/lib/analysis.ts"
      provides: "buildDashboardData accepting ParsedSnapshot[], all helpers typed to ParsedSnapshot"
      contains: "ParsedSnapshot"
    - path: "claude-usage-tracker/src/app/api/dashboard/route.ts"
      provides: "parseSnapshots call before buildDashboardData"
      contains: "parseSnapshots"
    - path: "claude-usage-tracker/test/analysis.test.ts"
      provides: "makeSnapshot returning ParsedSnapshot, all tests passing"
      contains: "ParsedSnapshot"
  key_links:
    - from: "claude-usage-tracker/src/app/api/dashboard/route.ts"
      to: "claude-usage-tracker/src/lib/analysis.ts"
      via: "buildDashboardData(parseSnapshots(querySnapshots(config)), ...)"
      pattern: "parseSnapshots"
    - from: "claude-usage-tracker/src/lib/analysis.ts"
      to: "claude-usage-tracker/src/lib/queries.ts"
      via: "import { parseSnapshots, type ParsedSnapshot } from ./queries"
      pattern: "ParsedSnapshot"
---

<objective>
Update `analysis.ts` to accept `ParsedSnapshot[]` instead of `SnapshotRow[]`, wire the call to `parseSnapshots()` in `dashboard/route.ts`, and update `analysis.test.ts` so the `makeSnapshot` factory produces `ParsedSnapshot` objects. This is the last code-change plan in Phase 1 — after it, `npm test` must be fully green and all dashboard panels must source data from `raw_json` via `queries.ts`.

Purpose: Closes the read path. Dashboard panels continue rendering correctly (UI-08) now that typed values come from `ParsedSnapshot` rather than dropped schema columns.
Output: Updated `analysis.ts`, updated `dashboard/route.ts`, updated `analysis.test.ts`. Full test suite green.
</objective>

<execution_context>
@/c/Users/2supe/.claude/get-shit-done/workflows/execute-plan.md
@/c/Users/2supe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation-db-refactor/01-CONTEXT.md
@.planning/phases/01-foundation-db-refactor/01-RESEARCH.md
@.planning/phases/01-foundation-db-refactor/01-PATTERNS.md

<interfaces>
<!-- ParsedSnapshot (from queries.ts) — what analysis.ts now receives -->
id: number
timestamp: string
status: string
endpoint: string | null
response_status: number | null
error_message: string | null
raw_json: string | null
five_hour_utilization: number | null
five_hour_resets_at: string | null
seven_day_utilization: number | null
seven_day_resets_at: string | null
extra_usage_enabled: boolean | null
extra_usage_monthly_limit: number | null
extra_usage_used_credits: number | null
extra_usage_utilization: number | null

<!-- Key: field names on ParsedSnapshot exactly match old SnapshotRow column names
     (minus auth_mode and the 8 dropped columns). So every place in analysis.ts that
     reads s.five_hour_utilization, s.extra_usage_used_credits, etc. requires no rename — 
     only the type annotation changes from SnapshotRow to ParsedSnapshot. -->

<!-- analysis.ts changes summary -->
Add import: import { parseSnapshots, type ParsedSnapshot } from "./queries";
Keep import: import type { SnapshotRow } from "./db"; (still used for health.lastSnapshot/lastSuccess/recentErrors)

buildDashboardData signature change:
  OLD: function buildDashboardData(snapshots: SnapshotRow[], storage, runtime)
  NEW: function buildDashboardData(snapshots: ParsedSnapshot[], storage, runtime)

All internal helpers that take SnapshotRow[] or SnapshotRow — change to ParsedSnapshot[] / ParsedSnapshot:
  buildActivity(snapshots: ParsedSnapshot[])
  buildUsageInsights(snapshots: ParsedSnapshot[])
  buildExtraUsageInsights(snapshots: ParsedSnapshot[])
  computeDelta(prev: ParsedSnapshot, curr: ParsedSnapshot, windowKey)

DashboardData.health fields stay as SnapshotRow (lastSnapshot, lastSuccess, recentErrors):
  The route handler passes raw SnapshotRow data for health separately.
  Option: pass the raw SnapshotRow[] as a second rawSnapshots parameter for health only,
  OR: since ParsedSnapshot has the same identity fields (id, timestamp, status, endpoint, response_status, error_message, raw_json),
  health can read from ParsedSnapshot directly — ParsedSnapshot IS compatible for health since all health fields are identity fields.
  Simplest approach: change health.lastSnapshot and health.lastSuccess types to ParsedSnapshot | null,
  and health.recentErrors to ParsedSnapshot[]. The DashboardData interface changes accordingly.
  If that causes downstream type errors in the UI components, revert to keeping SnapshotRow there and passing raw rows separately.
  Read the current buildDashboardData implementation to decide which approach introduces fewer changes.

computeDelta dynamic key access:
  The function accesses prev[windowKey + "_utilization"] dynamically.
  TypeScript may flag this on ParsedSnapshot. Use explicit field access or cast as needed.

<!-- dashboard/route.ts change -->
Add import: import { parseSnapshots } from "@/lib/queries";
Change: buildDashboardData(snapshots, ...) -> buildDashboardData(parseSnapshots(snapshots), ...)
Where snapshots = querySnapshots(config, ...) — the raw SnapshotRow[] from db.

<!-- analysis.test.ts change -->
Add import: import type { ParsedSnapshot } from "../src/lib/queries";
Change makeSnapshot return type from SnapshotRow to ParsedSnapshot.
Remove auth_mode from the default makeSnapshot values (it does not exist on ParsedSnapshot).
Remove import of SnapshotRow from db if it is no longer used directly in tests (or keep it if buildDashboardData
type signature requires it for the health fields).
All existing makeSnapshot({five_hour_utilization: 25, ...}) call sites continue to work — field names unchanged.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update analysis.ts and dashboard/route.ts to route through ParsedSnapshot</name>
  <read_first>
    - claude-usage-tracker/src/lib/analysis.ts (full file — understand buildDashboardData, all internal helpers, DashboardData.health type, computeDelta dynamic key access, safeParseJson usage)
    - claude-usage-tracker/src/app/api/dashboard/route.ts (full file — understand how it calls buildDashboardData and querySnapshots)
    - claude-usage-tracker/src/lib/queries.ts (confirm ParsedSnapshot interface and parseSnapshots export)
  </read_first>
  <files>
    claude-usage-tracker/src/lib/analysis.ts
    claude-usage-tracker/src/app/api/dashboard/route.ts
  </files>
  <action>
    Change 1 — analysis.ts imports:
    Add to the import section at the top:
      import { parseSnapshots, type ParsedSnapshot } from "./queries";
    Keep: import type { SnapshotRow } from "./db";  (still needed for health types in DashboardData)

    Change 2 — analysis.ts buildDashboardData signature:
    Change the snapshots parameter type from SnapshotRow[] to ParsedSnapshot[].
    Signature becomes: export function buildDashboardData(snapshots: ParsedSnapshot[], storage: ..., runtime: CollectorState): DashboardData

    Change 3 — analysis.ts internal helpers:
    Change all parameter types that were SnapshotRow or SnapshotRow[] to ParsedSnapshot / ParsedSnapshot[]:
      buildActivity(snapshots: ParsedSnapshot[])
      buildUsageInsights(snapshots: ParsedSnapshot[])
      buildExtraUsageInsights(snapshots: ParsedSnapshot[])
      computeDelta(prev: ParsedSnapshot, curr: ParsedSnapshot, windowKey: "five_hour" | "seven_day")

    Change 4 — analysis.ts computeDelta dynamic key access:
    The existing implementation accesses prev[windowKey + "_utilization"] using template literal indexing.
    If TypeScript rejects this on ParsedSnapshot (which lacks an index signature), replace with explicit access:
      const prevUtil = windowKey === "five_hour" ? prev.five_hour_utilization : prev.seven_day_utilization;
      const currUtil = windowKey === "five_hour" ? curr.five_hour_utilization : curr.seven_day_utilization;
      const prevReset = windowKey === "five_hour" ? prev.five_hour_resets_at : prev.seven_day_resets_at;
      const currReset = windowKey === "five_hour" ? curr.five_hour_resets_at : curr.seven_day_resets_at;
      return computeUsageDelta(prevUtil, currUtil, prevReset, currReset);

    Change 5 — analysis.ts health block:
    In buildDashboardData, the health block (lastSnapshot, lastSuccess, recentErrors) currently reads SnapshotRow fields.
    ParsedSnapshot includes all the same identity fields (id, timestamp, status, endpoint, response_status, error_message, raw_json).
    Change the DashboardData.health type to use ParsedSnapshot instead of SnapshotRow:
      lastSnapshot: ParsedSnapshot | null;
      lastSuccess: ParsedSnapshot | null;
      recentErrors: ParsedSnapshot[];
    This allows the health block to be computed from the same snapshots: ParsedSnapshot[] parameter without a separate raw array.
    If this change causes TypeScript errors in CollectorHealth.tsx (which reads health fields), check what fields it actually uses — if they are all identity fields, ParsedSnapshot is compatible and no component change is needed.

    Change 6 — Remove safeParseJson from analysis.ts if it is only used for the rawJson field:
    The rawJson field in DashboardData.current is built as: rawJson: safeParseJson(curr.raw_json)
    After the change, curr is a ParsedSnapshot and curr.raw_json still exists as a string. safeParseJson can remain in analysis.ts or be replaced with a direct call. Do not remove it if it is still used.

    Change 7 — dashboard/route.ts:
    Add import: import { parseSnapshots } from "@/lib/queries";
    Find the buildDashboardData call site. Wrap the querySnapshots result:
      const rawSnapshots = querySnapshots(config, { since: cutoff });
      const result = buildDashboardData(parseSnapshots(rawSnapshots), storageMeta, collectorState);
    If the current call is a single-expression call, extract to two lines as shown above.

    Code style: 2-space indent, double quotes, semicolons, named exports only.
  </action>
  <verify>
    <automated>cd "claude-usage-tracker" && npx tsc --noEmit 2>&1 | grep -v "node_modules" | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "ParsedSnapshot" claude-usage-tracker/src/lib/analysis.ts shows at least 4 matches (import + parameter types in buildDashboardData and helpers).
    - grep -n "parseSnapshots" claude-usage-tracker/src/app/api/dashboard/route.ts shows at least 1 match (import + call).
    - grep -n "SnapshotRow\[\]\|SnapshotRow," claude-usage-tracker/src/lib/analysis.ts shows SnapshotRow only for health type fields (if kept there) — NOT for buildDashboardData or any helper parameter.
    - cd claude-usage-tracker && npx tsc --noEmit 2>&1 | grep -v "node_modules" returns nothing or only errors in unrelated files (analysis.ts, queries.ts, dashboard/route.ts must be error-free).
    - grep -n "import.*parseSnapshots\|parseSnapshots.*import" claude-usage-tracker/src/app/api/dashboard/route.ts shows the import is present.
  </acceptance_criteria>
  <done>analysis.ts accepts ParsedSnapshot[], all internal helpers are typed to ParsedSnapshot, dashboard/route.ts wraps querySnapshots output with parseSnapshots before passing to buildDashboardData. TypeScript compiles both files without errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update analysis.test.ts makeSnapshot factory to return ParsedSnapshot; run full test suite</name>
  <read_first>
    - claude-usage-tracker/test/analysis.test.ts (full file — understand makeSnapshot factory, all test cases, and what fields they set)
    - claude-usage-tracker/src/lib/queries.ts (confirm ParsedSnapshot interface field names and types)
    - claude-usage-tracker/src/lib/analysis.ts (updated from Task 1 — confirm buildDashboardData now accepts ParsedSnapshot[])
  </read_first>
  <files>
    claude-usage-tracker/test/analysis.test.ts
  </files>
  <behavior>
    - All existing analysis.test.ts test cases continue to pass after the makeSnapshot factory change
    - makeSnapshot({five_hour_utilization: 25, five_hour_resets_at: "...", ...}) call sites require NO changes (field names identical on ParsedSnapshot)
    - extra_usage_enabled type changes from number | null to boolean | null — any test that passes extra_usage_enabled: 1 must be updated to extra_usage_enabled: true
    - The "auth_mode" field is removed from the makeSnapshot default object (it no longer exists on ParsedSnapshot)
    - Full npm test suite passes (exit 0)
  </behavior>
  <action>
    Make these specific changes to claude-usage-tracker/test/analysis.test.ts:

    Change 1 — Update imports:
    Add: import type { ParsedSnapshot } from "../src/lib/queries";
    Keep: import type { SnapshotRow } from "../src/lib/db"; only if still needed for type annotations elsewhere in the file. If buildDashboardData now takes ParsedSnapshot[], the test file may not need SnapshotRow at all — remove the import if unused.

    Change 2 — Update makeSnapshot factory function:
    Change the return type annotation from SnapshotRow to ParsedSnapshot.
    Change the overrides type from Partial<SnapshotRow> to Partial<ParsedSnapshot>.
    Remove auth_mode: "bearer" from the default object (it does not exist on ParsedSnapshot).
    Change extra_usage_enabled default from null to null (unchanged, still null).
    All other default values stay identical: status: "ok", endpoint: "test", response_status: 200, five_hour_utilization: null, etc.

    New makeSnapshot signature:
      function makeSnapshot(overrides: Partial<ParsedSnapshot> & { timestamp: string }): ParsedSnapshot {
        return {
          id: 1,
          status: "ok",
          endpoint: "test",
          response_status: 200,
          five_hour_utilization: null,
          five_hour_resets_at: null,
          seven_day_utilization: null,
          seven_day_resets_at: null,
          extra_usage_enabled: null,
          extra_usage_monthly_limit: null,
          extra_usage_used_credits: null,
          extra_usage_utilization: null,
          raw_json: null,
          error_message: null,
          ...overrides,
        };
      }

    Change 3 — Update any test call sites where extra_usage_enabled is set to a number (e.g., extra_usage_enabled: 1):
    Change to boolean: extra_usage_enabled: true.
    Scan the full file for "extra_usage_enabled:" and update each occurrence if it uses a number.

    Change 4 — After updating the factory and test calls, run the full test suite and fix any remaining type errors:
      cd claude-usage-tracker && npm test 2>&1

    The full suite includes: db.test.ts, queries.test.ts, analysis.test.ts, and any other test files. All must pass.
    If any test file other than the three in this phase still has TypeScript errors referencing old SnapshotRow fields, note them but do not fix them in this plan (they are out of scope — only db.test.ts, queries.test.ts, and analysis.test.ts are in scope).
  </action>
  <verify>
    <automated>cd "claude-usage-tracker" && npm test 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - cd claude-usage-tracker && npm test exits with code 0 (all tests pass, full suite green).
    - grep -n "ParsedSnapshot" claude-usage-tracker/test/analysis.test.ts shows at least 2 matches (import + return type annotation).
    - grep -n "auth_mode" claude-usage-tracker/test/analysis.test.ts returns nothing (removed from makeSnapshot default).
    - grep -n "makeSnapshot" claude-usage-tracker/test/analysis.test.ts shows the factory function definition and all existing call sites.
    - Output of npm test does NOT include "FAIL" or uncaught TypeScript errors for analysis.test.ts, db.test.ts, or queries.test.ts.
    - grep -c "SnapshotRow" claude-usage-tracker/test/analysis.test.ts returns 0 or 1 (SnapshotRow import removed if unused; only kept if referenced for DashboardData health type checks in a test).
  </acceptance_criteria>
  <done>analysis.test.ts uses ParsedSnapshot for makeSnapshot factory. All tests in the full npm test suite pass (exit 0). Phase 1 code changes are complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ParsedSnapshot data in analysis | Fields are derived from locally-stored raw_json; no external input touches analysis.ts |
| DashboardData JSON output | The JSON shape is unchanged; no new fields are exposed to the browser client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | buildDashboardData input | accept | Input is ParsedSnapshot[] computed from local SQLite; no user-controlled values reach the analysis layer directly |
| T-05-02 | Information Disclosure | DashboardData JSON | accept | JSON shape is unchanged from before; no new fields are surfaced in the API response |
| T-05-03 | Denial of Service | parseSnapshots in hot path | accept | parseSnapshots is O(n) pure transform; called once per dashboard request; bounded by snapshot count |
</threat_model>

<verification>
After both tasks complete — this is the Phase 1 gate:
1. cd claude-usage-tracker && npm test → exit 0 (all tests pass)
2. grep -c "SnapshotRow" claude-usage-tracker/src/lib/analysis.ts → 0 or 1 only for health type in DashboardData interface
3. grep -c "parseSnapshots" claude-usage-tracker/src/app/api/dashboard/route.ts → >= 1
4. grep -c "ParsedSnapshot" claude-usage-tracker/test/analysis.test.ts → >= 2
5. cd claude-usage-tracker && npx tsc --noEmit 2>&1 | grep -v "node_modules" → no errors
</verification>

<success_criteria>
- analysis.ts buildDashboardData accepts ParsedSnapshot[] (not SnapshotRow[]).
- dashboard/route.ts calls parseSnapshots(querySnapshots(config)) before buildDashboardData.
- analysis.test.ts makeSnapshot returns ParsedSnapshot, auth_mode removed.
- Full npm test suite passes (exit 0) — db.test.ts, queries.test.ts, analysis.test.ts all green.
- TypeScript compiles the entire project without errors (npx tsc --noEmit exit 0).
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-db-refactor/01-05-SUMMARY.md` following the summary template.
</output>
